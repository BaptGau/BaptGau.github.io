remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme

markdown: kramdown
kramdown:
  input: GFM
  # no math_engine: leave $...$ / $$...$$ untouched so MathJax 3 renders them client-side
  math_engine:
  syntax_highlighter: rouge
